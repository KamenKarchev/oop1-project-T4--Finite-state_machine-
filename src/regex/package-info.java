/**
 * <h2>Регулярни изрази (regex)</h2>
 *
 * <p>
 * Пакетът съдържа компоненти за работа с регулярни изрази. В проекта съществуват както по-стари
 * („legacy“) класове (напр. токенизация и преобразуване до postfix/Thompson), така и по-нов
 * парсер в пакета {@link machine.RegexParser}, който директно строи автомат.
 * </p>
 *
 * <p>
 * Ако дадени класове са маркирани като {@code @Deprecated}, те се считат за заменени и се
 * използват основно за референция/сравнение.
 * </p>
 */
package regex;

